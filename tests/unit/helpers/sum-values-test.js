import { sumValues } from 'earthwars/helpers/sum-values';
import { module, test } from 'qunit';

module('Unit | Helper | sum values');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sumValues([42]);
  assert.ok(result);
});
